% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_simulation.R
\name{run_mhrf_lss_simulation}
\alias{run_mhrf_lss_simulation}
\title{Run M-HRF-LSS Simulation and Validation}
\usage{
run_mhrf_lss_simulation(
  n_voxels = 500,
  n_timepoints = 300,
  n_trials = 20,
  n_conditions = 3,
  TR = 2,
  noise_levels = c(0, 2, 5, 10),
  hrf_variability = c("none", "moderate", "high"),
  manifold_params = list(),
  pipeline_params = list(),
  output_dir = tempdir(),
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{n_voxels}{Number of voxels to simulate (default 500)}

\item{n_timepoints}{Number of time points (default 300)}

\item{n_trials}{Number of trials per condition (default 20)}

\item{n_conditions}{Number of experimental conditions (default 3)}

\item{TR}{Repetition time in seconds (default 2.0)}

\item{noise_levels}{Vector of noise levels to test (DVARS as percentage, default c(0, 2, 5, 10))}

\item{hrf_variability}{Type of HRF variability: "none", "moderate", "high" (default "moderate")}

\item{manifold_params}{List of manifold construction parameters}

\item{pipeline_params}{List of pipeline parameters (lambda values, etc.)}

\item{output_dir}{Directory to save results (default tempdir())}

\item{seed}{Random seed for reproducibility (default 42)}

\item{verbose}{Print progress messages (default TRUE)}
}
\value{
A list containing:
\itemize{
\item \code{ground_truth}: List of true HRFs, betas, and parameters
\item \code{estimates}: List of estimated HRFs, betas from pipeline
\item \code{metrics}: Data frame of performance metrics
\item \code{noise_curves}: Performance vs noise level curves
\item \code{report_path}: Path to generated HTML report
}
}
\description{
Generates synthetic fMRI data with known ground truth and evaluates the
M-HRF-LSS pipeline performance across various metrics.
}
\details{
This function implements a comprehensive validation framework for
the M-HRF-LSS pipeline. It generates synthetic BOLD data with known HRF
shapes, trial amplitudes, and noise characteristics, then evaluates how
well the pipeline recovers these parameters.
}
\examples{
\dontrun{
# Run basic simulation
sim_results <- run_mhrf_lss_simulation(
  n_voxels = 200,
  n_timepoints = 250,
  noise_levels = c(0, 5, 10),
  hrf_variability = "moderate"
)

# View performance metrics
print(sim_results$metrics)

# Plot noise robustness curves
plot(sim_results$noise_curves)
}

}
