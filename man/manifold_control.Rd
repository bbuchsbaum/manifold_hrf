% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifold_control.R
\name{manifold_control}
\alias{manifold_control}
\title{Control Parameters for Manifold HRF Estimation}
\usage{
manifold_control(
  preset = c("balanced", "fast", "thorough"),
  m_manifold_dim = NULL,
  lambda_manifold = 0.1,
  num_neighbors_mfd = 75,
  lambda_spatial_smooth = 0.5,
  num_neighbors_Lsp = 6,
  lambda_gamma = 1e-06,
  lambda_condition_betas = 1e-04,
  max_iter = 100,
  tolerance = 1e-06,
  orthogonal_approx = FALSE,
  n_cores = 1,
  verbose_level = 1,
  ram_threshold_GB = 8,
  generate_qc_report = FALSE,
  ...
)
}
\arguments{
\item{preset}{Character string specifying a preset configuration:
\describe{
\item{"fast"}{Quick estimation with reduced iterations and neighbors}
\item{"balanced"}{Default settings balancing speed and accuracy}
\item{"thorough"}{High-quality estimation with more iterations}
}}

\item{m_manifold_dim}{Integer; dimension of the HRF manifold (NULL for automatic selection)}

\item{lambda_manifold}{Numeric; regularization for manifold construction}

\item{num_neighbors_mfd}{Integer; number of neighbors for manifold construction}

\item{lambda_spatial_smooth}{Numeric; spatial smoothing regularization strength}

\item{num_neighbors_Lsp}{Integer; number of neighbors for spatial graph}

\item{lambda_gamma}{Numeric; ridge penalty for voxel-wise fitting}

\item{lambda_condition_betas}{Numeric; regularization for condition-level betas}

\item{max_iter}{Integer; maximum iterations for optimization}

\item{tolerance}{Numeric; convergence tolerance}

\item{orthogonal_approx}{Logical; use orthogonal approximation in GLM}

\item{n_cores}{Integer; number of cores for parallel processing}

\item{verbose_level}{Integer; verbosity level (0 = silent, 1 = progress, 2 = detailed)}

\item{ram_threshold_GB}{Numeric; RAM threshold for memory strategy selection}

\item{generate_qc_report}{Logical; whether to generate quality control metrics}

\item{...}{Additional parameters to override preset values}
}
\value{
A list of class "manifold_control" containing all control parameters
}
\description{
Constructs a list of control parameters for the manifold HRF estimation algorithm.
Provides preset configurations and allows fine-grained control over all parameters.
}
\examples{
# Use balanced preset
ctrl <- manifold_control()

# Fast estimation
ctrl_fast <- manifold_control(preset = "fast")

# Custom settings
ctrl_custom <- manifold_control(
  preset = "balanced",
  max_iter = 200,
  lambda_spatial_smooth = 1.0
)

}
